// create the editor
/*
import { myData } from "./data.ts";
const container = document.getElementById("jsoneditor");
const container2 = document.getElementById("jsoneditor2");

const options = {};
const editor = new JSONEditor(container, options);
const editor2 = new JSONEditor(container2, options);

// set json
const initialJson = {
  Array: [1, 2, 3],
  Boolean: true,
  Null: null,
  Number: 123,
  Object: { a: "b", c: "d" },
  String: "Hello World",
};
const SetJson();

editor.set(myData);




// get json
setTimeout(() => {
  const updatedJson = editor.get();
  editor2.set(updatedJson);
}, 5000);


var map = L.map("map").setView([9.0563, 7.4985], 6);
L.tileLayer("leaflet/{z}/{x}/{y}.png", {
  crossOrign: false,
  maxZoom: 19,
  attribution:
    '&copy; <a href="http://www.openstreetmap/copyright">OpenStreetMap</a>',
}).addTo(map);

const geojsonFeatures = myData.features;
const layer=L.geoJSON(geojsonFeatures, {
  onEachFeature: function (feature, layer) {
    layer.bindPopup(
      "<h1>" + feature.properties.NAME + "</h1><p>id: " + feature.id + "</p>",
    );
  },
})
layer.addTo(map);
*/

